# AES
AES (Rijndael) è un algoritmo di cifratura a blocchi a chiave simmetrica adottato come standard negli USA. Opera su matrici “State” di byte 4×4 per blocchi da 128 bit; aumentando la lunghezza del blocco (fino a 256 bit) cresce il numero di colonne, pari ai bit/32.
